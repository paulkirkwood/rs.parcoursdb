extern crate parcoursdb;
extern crate chrono;

#[cfg(test)]
mod test {
    use parcoursdb::gent_wevelgem::*;
    use parcoursdb::classic::Classic;

    #[test]
    fn test_gent_wevelgem_1934() {
         let edition = gent_wevelgem_1934();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1935() {
         let edition = gent_wevelgem_1935();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1936() {
         let edition = gent_wevelgem_1936();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1937() {
         let edition = gent_wevelgem_1937();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1938() {
         let edition = gent_wevelgem_1938();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1939() {
         let edition = gent_wevelgem_1939();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1945() {
         let edition = gent_wevelgem_1945();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1946() {
         let edition = gent_wevelgem_1946();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1947() {
         let edition = gent_wevelgem_1947();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1948() {
         let edition = gent_wevelgem_1948();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1949() {
         let edition = gent_wevelgem_1949();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1950() {
         let edition = gent_wevelgem_1950();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1951() {
         let edition = gent_wevelgem_1951();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1952() {
         let edition = gent_wevelgem_1952();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1953() {
         let edition = gent_wevelgem_1953();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1954() {
         let edition = gent_wevelgem_1954();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1955() {
         let edition = gent_wevelgem_1955();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1956() {
         let edition = gent_wevelgem_1956();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1957() {
         let edition = gent_wevelgem_1957();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1958() {
         let edition = gent_wevelgem_1958();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1959() {
         let edition = gent_wevelgem_1959();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1960() {
         let edition = gent_wevelgem_1960();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1961() {
         let edition = gent_wevelgem_1961();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1962() {
         let edition = gent_wevelgem_1962();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1963() {
         let edition = gent_wevelgem_1963();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1964() {
         let edition = gent_wevelgem_1964();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1965() {
         let edition = gent_wevelgem_1965();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1966() {
         let edition = gent_wevelgem_1966();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1967() {
         let edition = gent_wevelgem_1967();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1968() {
         let edition = gent_wevelgem_1968();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1969() {
         let edition = gent_wevelgem_1969();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1970() {
         let edition = gent_wevelgem_1970();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1971() {
         let edition = gent_wevelgem_1971();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1972() {
         let edition = gent_wevelgem_1972();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1973() {
         let edition = gent_wevelgem_1973();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1974() {
         let edition = gent_wevelgem_1974();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1975() {
         let edition = gent_wevelgem_1975();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1976() {
         let edition = gent_wevelgem_1976();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1977() {
         let edition = gent_wevelgem_1977();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1978() {
         let edition = gent_wevelgem_1978();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1979() {
         let edition = gent_wevelgem_1979();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1980() {
         let edition = gent_wevelgem_1980();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1981() {
         let edition = gent_wevelgem_1981();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1982() {
         let edition = gent_wevelgem_1982();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1983() {
         let edition = gent_wevelgem_1983();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1984() {
         let edition = gent_wevelgem_1984();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1985() {
         let edition = gent_wevelgem_1985();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1986() {
         let edition = gent_wevelgem_1986();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1987() {
         let edition = gent_wevelgem_1987();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1988() {
         let edition = gent_wevelgem_1988();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1989() {
         let edition = gent_wevelgem_1989();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1990() {
         let edition = gent_wevelgem_1990();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1991() {
         let edition = gent_wevelgem_1991();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1992() {
         let edition = gent_wevelgem_1992();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1993() {
         let edition = gent_wevelgem_1993();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1994() {
         let edition = gent_wevelgem_1994();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1995() {
         let edition = gent_wevelgem_1995();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1996() {
         let edition = gent_wevelgem_1996();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1997() {
         let edition = gent_wevelgem_1997();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1998() {
         let edition = gent_wevelgem_1998();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_1999() {
         let edition = gent_wevelgem_1999();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_2000() {
         let edition = gent_wevelgem_2000();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_2001() {
         let edition = gent_wevelgem_2001();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_2002() {
         let edition = gent_wevelgem_2002();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_2003() {
         let edition = gent_wevelgem_2003();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_2004() {
         let edition = gent_wevelgem_2004();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_2005() {
         let edition = gent_wevelgem_2005();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_2006() {
         let edition = gent_wevelgem_2006();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_2007() {
         let edition = gent_wevelgem_2007();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_2008() {
         let edition = gent_wevelgem_2008();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_2009() {
         let edition = gent_wevelgem_2009();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_2010() {
         let edition = gent_wevelgem_2010();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_2011() {
         let edition = gent_wevelgem_2011();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_2012() {
         let edition = gent_wevelgem_2012();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_2013() {
         let edition = gent_wevelgem_2013();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Ghent (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_2014() {
         let edition = gent_wevelgem_2014();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Deinze (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_2015() {
         let edition = gent_wevelgem_2015();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Deinze (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_2016() {
         let edition = gent_wevelgem_2016();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Deinze (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_2017() {
         let edition = gent_wevelgem_2017();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Deinze (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_2018() {
         let edition = gent_wevelgem_2018();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Deinze (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }

    #[test]
    fn test_gent_wevelgem_2019() {
         let edition = gent_wevelgem_2019();
         assert_eq!(edition.name(), "Gent-Wevelgem");
         assert_eq!(edition.start().qualified_place(), "Deinze (Belgium)");
         assert_eq!(edition.finish().qualified_place(), "Wevelgem (Belgium)");
    }
}
