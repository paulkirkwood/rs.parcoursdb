extern crate parcoursdb;
extern crate chrono;

#[cfg(test)]
mod test {
    use parcoursdb::milano_san_remo::*;
    use parcoursdb::classic::{Classic,HillyClassic};

    #[test]
    fn test_milano_san_remo_1907() {
        let edition = milano_san_remo_1907();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1908() {
        let edition = milano_san_remo_1908();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1909() {
        let edition = milano_san_remo_1909();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1910() {
        let edition = milano_san_remo_1910();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1911() {
        let edition = milano_san_remo_1911();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1912() {
        let edition = milano_san_remo_1912();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1913() {
        let edition = milano_san_remo_1913();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1914() {
        let edition = milano_san_remo_1914();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1915() {
        let edition = milano_san_remo_1915();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1917() {
        let edition = milano_san_remo_1917();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1918() {
        let edition = milano_san_remo_1918();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1919() {
        let edition = milano_san_remo_1919();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1920() {
        let edition = milano_san_remo_1920();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1921() {
        let edition = milano_san_remo_1921();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1922() {
        let edition = milano_san_remo_1922();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1923() {
        let edition = milano_san_remo_1923();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1924() {
        let edition = milano_san_remo_1924();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1925() {
        let edition = milano_san_remo_1925();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1926() {
        let edition = milano_san_remo_1926();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1927() {
        let edition = milano_san_remo_1927();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1928() {
        let edition = milano_san_remo_1928();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1929() {
        let edition = milano_san_remo_1929();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1930() {
        let edition = milano_san_remo_1930();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1931() {
        let edition = milano_san_remo_1931();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1932() {
        let edition = milano_san_remo_1932();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1933() {
        let edition = milano_san_remo_1933();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1934() {
        let edition = milano_san_remo_1934();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1935() {
        let edition = milano_san_remo_1935();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1936() {
        let edition = milano_san_remo_1936();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1937() {
        let edition = milano_san_remo_1937();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1938() {
        let edition = milano_san_remo_1938();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1939() {
        let edition = milano_san_remo_1939();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1940() {
        let edition = milano_san_remo_1940();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1941() {
        let edition = milano_san_remo_1941();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1942() {
        let edition = milano_san_remo_1942();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1943() {
        let edition = milano_san_remo_1943();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1946() {
        let edition = milano_san_remo_1946();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1947() {
        let edition = milano_san_remo_1947();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1948() {
        let edition = milano_san_remo_1948();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1949() {
        let edition = milano_san_remo_1949();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1950() {
        let edition = milano_san_remo_1950();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1951() {
        let edition = milano_san_remo_1951();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1952() {
        let edition = milano_san_remo_1952();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1953() {
        let edition = milano_san_remo_1953();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1954() {
        let edition = milano_san_remo_1954();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1955() {
        let edition = milano_san_remo_1955();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1956() {
        let edition = milano_san_remo_1956();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1957() {
        let edition = milano_san_remo_1957();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1958() {
        let edition = milano_san_remo_1958();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1959() {
        let edition = milano_san_remo_1959();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1960() {
        let edition = milano_san_remo_1960();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1961() {
        let edition = milano_san_remo_1961();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1962() {
        let edition = milano_san_remo_1962();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1963() {
        let edition = milano_san_remo_1963();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1964() {
        let edition = milano_san_remo_1964();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1965() {
        let edition = milano_san_remo_1965();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1966() {
        let edition = milano_san_remo_1966();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1967() {
        let edition = milano_san_remo_1967();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1968() {
        let edition = milano_san_remo_1968();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1969() {
        let edition = milano_san_remo_1969();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1970() {
        let edition = milano_san_remo_1970();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1971() {
        let edition = milano_san_remo_1971();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1972() {
        let edition = milano_san_remo_1972();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1973() {
        let edition = milano_san_remo_1973();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1974() {
        let edition = milano_san_remo_1974();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1975() {
        let edition = milano_san_remo_1975();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1976() {
        let edition = milano_san_remo_1976();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1977() {
        let edition = milano_san_remo_1977();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1978() {
        let edition = milano_san_remo_1978();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1979() {
        let edition = milano_san_remo_1979();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1980() {
        let edition = milano_san_remo_1980();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1981() {
        let edition = milano_san_remo_1981();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1982() {
        let edition = milano_san_remo_1982();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1983() {
        let edition = milano_san_remo_1983();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1984() {
        let edition = milano_san_remo_1984();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1985() {
        let edition = milano_san_remo_1985();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1986() {
        let edition = milano_san_remo_1986();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1987() {
        let edition = milano_san_remo_1987();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1988() {
        let edition = milano_san_remo_1988();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1989() {
        let edition = milano_san_remo_1989();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1990() {
        let edition = milano_san_remo_1990();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1991() {
        let edition = milano_san_remo_1991();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1992() {
        let edition = milano_san_remo_1992();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1993() {
        let edition = milano_san_remo_1993();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1994() {
        let edition = milano_san_remo_1994();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1995() {
        let edition = milano_san_remo_1995();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1996() {
        let edition = milano_san_remo_1996();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1997() {
        let edition = milano_san_remo_1997();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1998() {
        let edition = milano_san_remo_1998();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_1999() {
        let edition = milano_san_remo_1999();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_2000() {
        let edition = milano_san_remo_2000();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_2001() {
        let edition = milano_san_remo_2001();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_2002() {
        let edition = milano_san_remo_2002();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_2003() {
        let edition = milano_san_remo_2003();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_2004() {
        let edition = milano_san_remo_2004();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_2005() {
        let edition = milano_san_remo_2005();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_2006() {
        let edition = milano_san_remo_2006();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_2007() {
        let edition = milano_san_remo_2007();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_2008() {
        let edition = milano_san_remo_2008();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_2009() {
        let edition = milano_san_remo_2009();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_2010() {
        let edition = milano_san_remo_2010();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_2011() {
        let edition = milano_san_remo_2011();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_2012() {
        let edition = milano_san_remo_2012();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_2013() {
        let edition = milano_san_remo_2013();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_2014() {
        let edition = milano_san_remo_2014();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_2015() {
        let edition = milano_san_remo_2015();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_2016() {
        let edition = milano_san_remo_2016();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_2017() {
        let edition = milano_san_remo_2017();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }

    #[test]
    fn test_milano_san_remo_2018() {
        let edition = milano_san_remo_2018();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");

        let cotes = [
            "151.8 km,Passo del Turchino,532m",
            "54.5 km,Capo Mele,67m",
            "46.6 km,Capo Cervo,61m",
            "41.4 km,Capo Berta,130m",
            "24.5 km,Cipressa,239m",
            "8.4 km,Poggio,160m"
        ];

        assert_eq!(edition.cotes(), cotes);
    }

    #[test]
    fn test_milano_san_remo_2019() {
        let edition = milano_san_remo_2019();
        assert_eq!(edition.name(), "Milano-San Remo");
        assert_eq!(edition.start().qualified_place(), "Milano (Italy)");
        assert_eq!(edition.finish().qualified_place(), "San Remo (Italy)");
    }
}
